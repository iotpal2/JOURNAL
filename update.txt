ALTER TABLE JOURNALHISTORY
  ADD COLUMN change_ts TIMESTAMP NOT NULL
  DEFAULT CURRENT TIMESTAMP;
  
UPDATE JOURNALHISTORY
   SET change_ts = TIMESTAMP(change_date, '00:00:00');
   
ALTER TABLE JOURNALHISTORY DROP COLUMN change_date;
ALTER TABLE JOURNALHISTORY
  RENAME COLUMN change_ts TO change_date;
  
ALTER TABLE JOURNALHISTORY
  ALTER COLUMN change_date SET NOT NULL;
  
CREATE OR REPLACE PROCEDURE MYLIB.MYPROC (
    IN  p_id   INT,
    OUT p_name CHAR(50)
)
LANGUAGE SQL
BEGIN
    -- your new procedure body
    SELECT name INTO p_name FROM customers WHERE id = p_id;
END;

DROP PROCEDURE MYLIB.MYPROC;
